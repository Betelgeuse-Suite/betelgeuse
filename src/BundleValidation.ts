
export const bundleValidation = () => {
  // If any of the following is not preset then not valid

  // betelgeuse.json not present
  // betelgeuse.json not valid
  //     -- needs a name, a cd and a version at the very least

}